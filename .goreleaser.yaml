version: 2

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    main: ./cmd/loam
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    binary: loam
    ldflags:
      - -s -w -X github.com/aretw0/loam.Version={{.Version}}

archives:
  - formats:
      - tar.gz
    # this name template makes the OS and Arch compatible with the results of uname.
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows
    format_overrides:
      - goos: windows
        formats:
          - zip

release:
  github:
    owner: aretw0
    name: loam
  draft: true
  prerelease: auto

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^refactor:"
      - "^style:"
      - "^Merge pull request"
      - "^Merge branch"
  groups:
    - title: Features
      regexp: "^.*feat((\\([\\w\\-\\.]+\\))?):"
      order: 0
    - title: "Bug Fixes"
      regexp: "^.*fix((\\([\\w\\-\\.]+\\))?):"
      order: 1
    - title: "Performance Improvements"
      regexp: "^.*perf((\\([\\w\\-\\.]+\\))?):"
      order: 2
    - title: "Others"
      order: 999

brews:
  - repository:
      owner: aretw0
      name: homebrew-tap
    directory: Formula
    homepage: "https://github.com/aretw0/loam"
    description: "An Embedded Reactive & Transactional Engine for Content & Metadata."
    license: "AGPL-3.0"

scoops:
  - repository:
      owner: aretw0
      name: scoop-bucket
    homepage: "https://github.com/aretw0/loam"
    description: "An Embedded Reactive & Transactional Engine for Content & Metadata."
    license: "AGPL-3.0"

winget:
  - publisher: aretw0
    publisher_url: "https://github.com/aretw0"
    publisher_support_url: "https://github.com/aretw0/loam/issues"
    package_identifier: "aretw0.loam"
    name: "loam"
    homepage: "https://github.com/aretw0/loam"
    license: "AGPL-3.0"
    license_url: "https://github.com/aretw0/loam/blob/main/LICENSE.txt"
    short_description: "An Embedded Reactive & Transactional Engine for Content & Metadata."
    repository:
      owner: aretw0
      name: winget-pkgs